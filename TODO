There are no sanity checks or maintenance. If left unchecked this would fill up the allotted datastore space.
There is "cleanup" code scattered around that I used to clean the datastore of duplicate/bad data that got inserted as I was learning + writing the insert stuff.
Allow for user selectable chart styles.
